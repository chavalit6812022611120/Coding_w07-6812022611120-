#include <stdio.h>

int main() {
    int level;

    // รับข้อมูลระดับสมาชิกจากผู้ใช้
    printf("กรุณาป้อนระดับสมาชิกของคุณ (1=Silver, 2=Gold, 3=Platinum, 4=Diamond): ");
    scanf("%d", &level);

    printf("สิทธิประโยชน์ของคุณคือ: ");

    // ใช้ switch-case ในการตรวจสอบเงื่อนไข
    switch (level) {
        case 1:
            printf("5%% discount\n");
            break; // หยุดการทำงานเมื่อจบ case
        case 2:
            printf("10%% discount + Reward points\n");
            break;
        case 3:
            printf("15%% discount + Reward points + Birthday gift\n");
            break;
        case 4:
            printf("ได้ทุกอย่าง + VIP events\n");
            break;
        default:
            // กรณีที่ค่าไม่ตรงกับ case ไหนเลย
            printf("ไม่พบระดับสมาชิกที่ระบุ\n");
            break;
    }

    return 0;
}

//* if-else (ยืดหยุ่น): ใช้เมื่อเงื่อนไขซับซ้อน หรือต้องเปรียบเทียบเป็นช่วง
  // เช่น ถ้าคะแนน >= 80 หรือ ถ้าผู้ใช้เป็นแอดมิน และ ล็อกอินอยู่
// switch (กระชับ): ใช้เมื่อต้องเปรียบเทียบ ตัวแปรเดียว กับค่าที่เจาะจงหลายๆ ค่า
  // เช่น กรณีที่โหมด = 'dark' หรือ กรณีที่วันในสัปดาห์ = 'วันจันทร์'
//ุปง่ายๆ: ถ้าเงื่อนไขมี มากกว่า/น้อยกว่า หรือซับซ้อน ให้ใช้ if ครับ แต่ถ้าเป็นการเช็คว่า "ตัวแปรนี้เท่ากับค่าไหนในลิสต์" ให้ใช้ switch จะอ่านง่ายกว่าครับ
//ตัวอย่าง if-else (เหมือนการตัดสินใจที่ซับซ้อน)
//มติว่าเราจะเลือกเสื้อผ้าใส่ตอนเช้า เราจะคิดเป็นเงื่อนไขที่ซับซ้อนแบบนี้:
//"ถ้า อุณหภูมิ สูงกว่า 30 องศา และ แดดออก... ให้ ใส่เสื้อยืด"
//"แต่ถ้า อุณหภูมิ น้อยกว่า 20 องศา... ให้ เอาเสื้อคลุมไปด้วย"
//"นอกเหนือจากนั้น... ก็ ใส่เสื้อเชิ้ตธรรมดาพอ"
//
//เห็นว่าเงื่อนไขมันหลากหลาย มีทั้ง 'มากกว่า/น้อยกว่า' และมีเงื่อนไข 'และ' มาเกี่ยวด้วย แบบนี้แหละครับคือการคิดแบบ if-else
//วอย่าง switch (เหมือนการเลือกจากเมนู)
//นี้ลองนึกภาพว่าเราไปสั่งกาแฟที่ร้าน พนักงานจะถามว่ารับอะไรดี เราก็ตอบไปอย่างเดียว เช่น "ลาเต้"
//องของพนักงานจะทำงานแบบ switch ทันที:
//"ลูกค้าสั่งอะไรมานะ?" (นี่คือตัวแปรเดียวที่เขาสนใจ)
// * กรณีที่เป็น "อเมริกาโน่"... ก็ไปทำอเมริกาโน่
// * กรณีที่เป็น "ลาเต้"... ก็ไปทำลาเต้ (เจอแล้ว!)
// * กรณีที่เป็น "คาปูชิโน่"... ก็ไปทำคาปูชิโน่
//
//แบ้คือการเอา ตัวแปรเดียว (คือเมนูกาแฟของเรา) ไปเทียบกับตัวเลือกต่างๆ ที่มีในหัว ถ้าตรงกับอันไหน ก็ทำอันนั้นเลย นี่แหละครับคือ switch ชัดเจนและตรงไปตรงมา